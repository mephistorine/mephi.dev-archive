---
layout: layouts/base.njk
---

<div class="container">
  <section class="hero">
    <h1 class="title">Привет 👋</h1>
    <p class="description">Тут я делюсь своим опытом, в разработке приложений и не только. Технический стек <a href="https://angular.io">Angular</a>, <a href="https://www.typescriptlang.org">Typescript</a>,
      <a href="https://rxjs.dev">RxJS</a> и <a href="https://leafletjs.com/">Leaflet</a></p>
  </section>

  <section class="section recent-articles">
    <h2 class="title">Свежее</h2>
    <ul class="article-list">
      {% for article in collections.articles | head(-3) | reverse %}
      <li><a href="{{ article.url }}">{{ article.data.title }}</a></li>
      {% endfor %}
    </ul>
  </section>

  <section class="section">
    <h2 class="title">Всё</h2>

    {% for year, articles in collections.articlesByYear %}
    <section class="section">
      <h3 class="title">{{ year }}</h3>
      <ul class="article-list">
        {% for article in articles %}
        <li><a href="{{ article.url }}">{{ article.data.title }}</a></li>
        {% endfor %}
      </ul>
    </section>
    {% endfor %}
  </section>
</div>
